//check number is maximum using pointer
#include<stdio.h>
void main()
{
    int *ptr1;
    int *ptr2;
    int a,b;
    ptr1=&a;
    ptr2=&b;
    printf("Enter two number ");
    scanf("%d%d",ptr1,ptr2);
if(*ptr1>*ptr2)
printf("%d is maximum ",*ptr1);
else
printf("%d is maximum ",*ptr2);
}
