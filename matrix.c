#include<stdio.h>
#include<stdlib.h>
void(main)
{
    int a1[][];
    int a2[][];
    int multi[][];
    int r1,c,r2;
    printf("Enter the first matrix");
    scanf("%d%d",&r1,&c);
    printf("Enter the second matrix");
    scanf("%d",&r2);
    printf("First matrix element is ");
    for(i=0;i<r;i++)
    {
        for(j=0;j<c;j++)
        {
            scanf("%d",&a1[i][j]);
        }
    }
    for(j=0;i<r;j++)
    {
        for(k=0;j<c;k++)
        {
            scanf("%d",&a1[j][k]);
        }
    }
    multi[][]
    
}